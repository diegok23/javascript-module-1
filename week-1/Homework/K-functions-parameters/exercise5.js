// Declare your function here
function createLongGreeting(string, number) {
  return `Hello, my name is ${string} and I'm ${number} years old`;
}
const greeting = createLongGreeting("Diego", 43);

console.log(greeting);
